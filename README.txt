Tout d'abord se rendre à l'adresse :
http://www-etu-info.iut2.upmf-grenoble.fr/~duchainj/projet_php/controller/afficher_flux.php

qui va lancer le controlleur de flux.
Dans la base de données, il y a par defaut le flux RSS du site LeMonde.fr
Si vous voulez rajouter un ou plusieurs flux, il faut le/les rajouter via la page principale
Si vous voulez supprimer un ou plusieurs flux, il faut remplir la zone de texte avec le titre d'un flux
Le controlleur principale se lance automatiquement
